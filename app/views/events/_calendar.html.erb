<%= month_calendar table: {class: "table table-bordered calendar"}, events: @events do |date, events| %>

		<%= date.strftime("%d") %>
		<ul class="list-group ">
		<% events.each do |e| %>
			<li class="list-group-item"><%= link_to e.event_type.name, event_path(e) %></li>
		<% end %>
		</ul>
<% end %>
